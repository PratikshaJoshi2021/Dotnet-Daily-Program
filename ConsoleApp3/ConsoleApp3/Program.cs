using System;

namespace ConsoleApp3
{
    class Program
    {
        static void Main(string[] args)
        {
            int[] arr = { 10, 20, 30 };
            for(int i = 0; i < arr.Length; i++)
            Console.WriteLine(arr[i]);
        }
    }
}
